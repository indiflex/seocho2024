import Input from "./Input";

const meta = {
  component: Input,
  title: "Atomic Input",
  tags: ["autodocs"],
};

export const DefaultInput = {
  args: {
    label: "DefaultInput",
  },
};

export default meta;
